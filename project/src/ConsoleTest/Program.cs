// This project is special. If you want to add new files you have to make sure
// They're included in the .csproj of this file.

// Make sure the references are correct.

// Make sure the Properties\launchSettings.json maps correctly
// Make sure the ConsoleTest references to the source generator works correctly

using ConsoleTest;

Console.WriteLine("Hello, World!");


var a = new InputMappings();
var b = new AudioPaths();